"""Consumer-owned interfaces required by the inner layers.

Ports are defined by the code that needs them and implemented by adapters (ADR 0011).
Repositories, clocks, identifier generation, schedulers, executors, secret resolution,
notifications, and audit sinks live here.

Populated from Wave 2.
"""
