"""Domain engine: entities, value objects, invariants, and pure decision policies.

This package imports only the Python standard library. Nothing here may import a
framework, a database session, or a vendor SDK — enforced by
``tests/unit/test_architecture.py``.

Populated in Wave 1.
"""
